'use client'

type Props = {
  selected?: string
  onChange?: (value: string) => void
}

export default function CategoryFilter({ selected, onChange }: Props) {
  // Retourne null ou un simple message pour l'instant
  return null
}
